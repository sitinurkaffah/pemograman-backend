const fruits = ["Banana", "Apple", "Orange"];

console.log(fruits[0]);
console.log(fruits[2]);

// lopping array menggunakan for of

for(const fruit of fruits) {
    console.log(`Buah ${fruit}`);
}